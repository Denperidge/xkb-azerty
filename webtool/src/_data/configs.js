import {readFileSync, readdirSync} from "node:fs";
import {} from "../../../data/";

const yazi = readFileSync("../vendor/yazi-keymap-default.toml")

function parseYazi(content) {
    [
        ['"1"'],
    ]

}

export default function (configData) {

    
    return {yazi}
    
}
